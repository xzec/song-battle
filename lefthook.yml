pre-commit:
  parallel: true
  jobs:
    - name: tsc
      run: pnpm --filter @repo/web typecheck

    - name: eslint
      run: pnpm --filter @repo/web lint

    - name: biome
      run: pnpm -r --parallel biome:check:staged
